# Development and CI-gate tooling. Not installed into the runtime image.
#
# Regenerate with:
#   pip-compile requirements-dev.in -o requirements-dev.txt --strip-extras
#
# Install runtime dependencies first, using their exact compiled pins.
-r requirements.txt

pytest>=8
pytest-cov>=5

# Tests use httpx directly.
httpx>=0.27

# Evaluation fixtures are YAML.
PyYAML>=6

# Deployment gate: dependency audit.
pip-audit>=2.7